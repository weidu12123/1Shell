services:
  1shell:
    build: .
    container_name: 1shell
    restart: unless-stopped
    ports:
      - "${PORT:-3301}:3301"
    volumes:
      - ./data:/app/data
      # 将宿主机的 AI CLI 工具挂载进容器（只读），无需在镜像内安装
      # claude-code：宿主机 npm install -g @anthropic-ai/claude-code 后自动生效
      - /root/.claude:/root/.claude
      - /usr/local/bin/claude:/usr/local/bin/claude:ro
      - /usr/local/lib/node_modules/@anthropic-ai:/usr/local/lib/node_modules/@anthropic-ai:ro
    env_file:
      - .env
    environment:
      - NODE_ENV=production
