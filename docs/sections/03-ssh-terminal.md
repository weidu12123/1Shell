# 板块三：SSH 终端

---

## 1. 界面布局

1Shell 的主界面分为三个区域：

![完整工作区](images/03-workspace-overview.png)

- **左侧边栏**：导航菜单（主控、程序、容器、网站、脚本、仓库、创作、AI、探针、审计）+ 主机列表 + 文件浏览器
- **中间区域**：终端主体
- **右侧面板**：AI 运维助手（可展开/收起）

---

## 2. 本地终端

登录后默认进入本机终端，即 1Shell 服务所在机器的 Shell。

![终端界面](images/03-terminal-fullscreen.png)

- Windows 系统显示 CMD / PowerShell
- Linux 系统显示 Bash / Zsh
- 状态栏显示「已连接」（绿色圆点）

---

## 3. 远程 SSH 终端

在左侧主机列表中点击任意远程主机的「切换」按钮，即可 SSH 连接到该主机。

![远程终端](images/03-terminal-remote.png)

连接后：
- 终端标签页自动切换到该主机
- 顶部显示主机名称和实时指标（CPU、内存、负载、磁盘）
- 终端内容为远程主机的 Shell 会话

---

## 4. 多标签页

### 4.1 标签页管理

- 每台主机对应一个终端标签页
- 点击 **「+ 新终端」** 可打开新的本地终端标签页
- 点击标签页名称切换不同终端
- 多个终端之间互不干扰，可同时在不同服务器上操作

### 4.2 多 VPS 同时管理

通过标签页可以快速在多台 VPS 之间切换。左侧主机列表显示所有主机的连接状态，点击即切换。

---

## 5. 顶部信息栏

![顶部状态栏](images/03-terminal-topbar.png)

终端上方的信息栏从左到右依次显示：

| 区域 | 内容 |
|------|------|
| **主机标识** | 当前主机名称（如「本机」） |
| **实时指标** | CPU 使用率、内存使用率、负载、磁盘使用率 |
| **功能按钮** | AI Agent、1Shell AI、AI 配置、AI 展开、设置、退出 |

---

## 6. 终端工具栏

终端状态栏右侧提供以下工具按钮：

| 按钮 | 功能 |
|------|------|
| **脚本** | 打开脚本库，选择脚本注入终端执行 |
| **Playbook** | 运行一次性 AI Playbook |
| **重连** | 断线后重新连接当前会话 |
| **AI 命令** | 打开 AI 命令建议面板 |
| **清屏** | 清除终端屏幕内容 |
| **全屏** | 终端全屏显示，隐藏侧边栏和顶栏 |
| **退出** | 断开当前终端会话 |

---

## 7. 跳板机级联（ProxyJump）

![跳板机设置](images/03-proxyjump.png)

当目标机器在 NAT 内网，无法从公网直接 SSH 连接时，可通过跳板机级联：

1. 先在 1Shell 中添加一台公网可达的主机作为「跳板机」
2. 添加内网目标主机时，在「中继跳板」下拉框中选择跳板机
3. 1Shell 会自动通过跳板机建立 SSH 隧道，连接到内网目标

连接路径：`1Shell → 跳板机（公网） → 目标机器（内网）`

---

## 8. 左侧导航栏

左侧竖向导航栏提供以下页面入口：

| 图标 | 页面 | 说明 |
|------|------|------|
| 主控 | 主控台 | 主机列表、添加主机 |
| 程序 | 运维程序 | Program 管理 |
| 容器 | 容器管理 | Docker 容器管理 |
| 网站 | 网站任意门 | 业务网址快捷入口 |
| 脚本 | 脚本库 | 常用脚本管理 |
| 仓库 | 仓库 | 代码仓库 |
| 创作 | 创作工作台 | AI 创作 Program/Skill/Playbook |
| AI | AI 设置 | AI 功能配置 |
| 探针 | 探针中心 | 主机监控数据 |
| 审计 | 审计日志 | 操作记录查询 |
| 退出 | 退出登录 | 退出当前账号 |

---

## 9. 常用操作

### 9.1 复制粘贴

- **复制**：选中终端中的文本，自动复制到剪贴板（或 Ctrl+Shift+C）
- **粘贴**：Ctrl+Shift+V 或右键粘贴

### 9.2 断线重连

网络中断后终端会显示断开状态，点击工具栏的「重连」按钮恢复会话。

### 9.3 终端大小自适应

终端会自动适应浏览器窗口大小，调整窗口时终端列数和行数自动更新。
