node_modules/
.env
*.log
data/*.db
data/*.db-shm
data/*.db-wal
data/*.log
data/*.json
data/bridge-token.json
data/audit.log
.DS_Store
Thumbs.db
test-results/
*.pyc
__pycache__/

# Debug / 临时文件
_debug*.txt
_debug*.js
_rescue_logs*.txt
*.tmp

# 部署脚本（含服务器凭据，仅本地使用）
deploy.js

# 内部规格/设计文档（不对外发布）
.spec-workflow/

# Claude Code 本地配置
.claude/

# 后端 agent-panel 运行时工作目录
data/cli-sandbox/

# 用户私有 program 配置（含 host UUID 等）
data/programs/

# 外部 Claude Code Skill 托管仓库（运行时 clone，不入库）
data/claude-code-skills/

# 视觉审查截图（不入库）
/*.png

# Playwright MCP 调试产物
.playwright-mcp/

# Release 打包产物
release/
